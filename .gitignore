Python virtual environmentThis folder contains thousands of files specific to your machine and should never be committed.venv/.venv/Python cache filesThese are temporary files generated by Python to speed up execution.pycache/*.pycIDE and editor specific filesThese are configuration files for your code editor (like VS Code)..vscode/.idea/Operating System generated filesThese are system files that don't belong in the project..DS_StoreThumbs.dbEnvironment variablesIt's a security best practice to never commit files containing secrets or API keys..env.env.*Jupyter Notebook checkpoints.ipynb_checkpointsBuild and distribution artifactsbuild/dist/*.egg-info/